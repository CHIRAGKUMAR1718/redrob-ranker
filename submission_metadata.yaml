# Redrob Hackathon — Submission Metadata

# ============================================================================
# Team identity
# ============================================================================
team_name: "CHIRAGKUMAR1718"

primary_contact:
  name: "Chirag Kumar"
  email: "primary@example.com"
  phone: "+91-XXXXXXXXXX"

team_members:
  - name: "Chirag Kumar"
    email: "primary@example.com"
    role: "Team Lead"

# ============================================================================
# Code and reproducibility
# ============================================================================
github_repo: "https://github.com/CHIRAGKUMAR1718/redrob-ranker"

sandbox_link: "https://huggingface.co/spaces/CHIRAGKUMAR1718/redrob-ranker"

reproduce_command: "python rank.py --candidates candidates.jsonl --out submission/team_001.csv"

# ============================================================================
# Compute environment
# ============================================================================
compute:
  platform: "Local Windows PC"
  cpu_cores: 8
  ram_gb: 16
  python_version: "3.13.1"
  os: "Windows 11"
  uses_gpu_for_inference: false
  has_network_during_ranking: false
  pre_computation_required: false
  pre_computation_time_minutes: 0

# ============================================================================
# AI tools declaration
# ============================================================================
ai_tools_used:
  - "Claude"

ai_usage_summary: |
  Used Claude for architecture discussion, code review and step by step implementation. No candidate data was fed to Claude.

# ============================================================================
# Approach summary
# ============================================================================
methodology_summary: |
  Rule-based ranker with explicit reasoning capture. Five scoring components (career trajectory, skills, experience, location, education) combined with a behavioral signal multiplier and text intelligence layer. Honeypot detection removes fake profiles. Runtime is approximately 3 minutes for 100K candidates on CPU only.

# ============================================================================
# Declarations
# ============================================================================
declarations:
  read_submission_spec: true
  code_is_original_work: true
  no_collusion: true
  honeypot_check_done: true
  reproduction_tested: true
